syntax = "proto3";

package foobar.auth;

message Auth {
    string username = 1;
    string password = 2;
}

message Token {
    string username = 1;
    string token = 2;
}

service Auths {
    rpc check_post(Auth) returns (Token) {};
}