syntax = "proto3";

import "wall.proto";
import "user.proto";

package foobar.profile;

service Profile {
    rpc get_user(user.User) returns (user.UserInfo) {};
    rpc get_wall(wall.WallQuery) returns (wall.WallPost) {};
}
