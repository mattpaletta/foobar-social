syntax = "proto3";

import "auth.proto";

package foobar.tokenizer;

service TokenDispenser {
    rpc create_token(auth.Auth) returns (auth.Token) {};
    rpc check_token(auth.Token) returns (auth.Token) {};
    rpc get_username(auth.Token) returns (auth.Token) {};
}